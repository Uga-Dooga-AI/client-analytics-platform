[phases.setup]
nixPkgs = ["...", "python312", "zlib"]

[phases.install]
cmds = [
  "...",
  "python3 -m venv --copies .venv",
  ".venv/bin/python -m pip install --upgrade pip",
  ".venv/bin/python -m pip install --no-cache-dir numpy==1.26.4 scipy==1.11.4 lmfit==1.3.1",
]
