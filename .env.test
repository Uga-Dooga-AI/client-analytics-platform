# Firebase Emulator environment for E2E tests
# Start emulators before running: firebase emulators:start

NEXT_PUBLIC_FIREBASE_PROJECT_ID=demo-test
NEXT_PUBLIC_FIREBASE_API_KEY=demo-key
NEXT_PUBLIC_FIREBASE_AUTH_EMULATOR_HOST=localhost:9099

FIREBASE_PROJECT_ID=demo-test
FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
FIRESTORE_EMULATOR_HOST=localhost:8080

# Bootstrap key for super_admin seeding in tests
SUPERADMIN_BOOTSTRAP_KEY=test-bootstrap-key-1234
